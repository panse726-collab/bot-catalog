# План улучшения общего дизайна бота (без переписывания всего кода)

## Что видно по текущей реализации

1. **Логика и тексты интерфейса сильно смешаны в `bot.php`**: сообщения, клавиатуры и бизнес-ветки находятся в одном файле, что затрудняет единый стиль и быстрые правки UX-копирайта.
2. **Тон сообщений разный**: где-то коротко и нейтрально (`"Активно?"`), где-то более человечно (`"Фото загружено ✅"`), из-за чего опыт выглядит неровным.
3. **Много «точечных» экранов без общего шаблона**: списки, карточка товара, редактирование и добавление используют разные паттерны подсказок/навигации.
4. **Сильная сторона уже есть**: хорошие inline-кнопки, пагинация и пошаговый ввод — это отличная база для «дизайн-апгрейда без тотальной переделки».

## Лучший вариант с минимальными правками

### Вариант: «UI-слой поверх текущей логики»

Вместо переписывания архитектуры — добавить тонкий слой единообразного интерфейса:

- вынести тексты в один файл `inc/ui_texts.php`;
- вынести все клавиатуры в `inc/ui_keyboards.php`;
- в `bot.php` оставить только маршрутизацию состояний и действия.

Это даст:

- единый голос/тон бота;
- быстрые изменения дизайна флоу без риска сломать Sheets-логику;
- ускорение дальнейших улучшений (онбординг, подсказки, сокращение шагов).

## Конкретные дизайн-улучшения (без изменения бизнес-логики)

### 1) Единый стиль сообщений

Ввести 4 типа системных сообщений:

- `info` — обычные шаги;
- `success` — подтверждения;
- `warn` — мягкие ошибки;
- `error` — критические ошибки.

Пример:

- Сейчас: `Ошибка публикации: ...`
- Предложение: `❌ Не удалось опубликовать товар. Причина: ...`

### 2) Унификация CTA и навигации

В каждом сценарии добавить одинаковые кнопки нижнего уровня:

- `⬅️ Назад`
- `🏠 Главное меню`
- `❌ Отмена`

Сейчас часть экранов это делает, часть — нет.

### 3) Улучшение флоу добавления товара

Без изменения схемы полей:

- перед стартом показывать короткий прогресс: `Шаг 1/12`;
- после каждого заполненного поля — авто-подтверждение `✅ {Поле} сохранено`;
- на этапе фото сразу показывать: `Можно отправить до 4 фото (минимум 1, если хотите отображать товар в каталоге)`.

### 4) Карточка товара в «псевдо-блочном» формате

Привести вывод к одинаковой сетке:

- заголовок;
- статус;
- цена;
- ключевые атрибуты категории;
- последняя строка с действиями.

Это визуально ощущается как «премиум» даже в обычном Telegram UI.

## Рекомендуемый порядок внедрения (быстрый и безопасный)

1. **Шаг 1 (1–2 часа):** вынос текстов в отдельный словарь + унификация копирайта.
2. **Шаг 2 (1–2 часа):** вынос клавиатур в отдельный модуль + одинаковая навигация.
3. **Шаг 3 (2–3 часа):** прогресс и подтверждения в add/edit-флоу.
4. **Шаг 4 (по желанию):** A/B-полировка формулировок для админов.

## Почему это лучший вариант именно для вашего кода

- текущий код уже рабочий и закрывает ключевые сценарии;
- полный рефактор сейчас не обязателен и рискован по времени;
- предложенный подход улучшает «дизайн-ощущение» быстро, сохраняя существующую интеграцию с Google Sheets и загрузкой фото.

## Короткий итог

Если цель — **сильно улучшить общий дизайн, не трогая весь код**, лучший путь:

> добавить тонкий UI-слой (тексты + клавиатуры + единый тон) и пройтись по основным сценариям add/list/edit точечными правками.

Это даст максимальный визуально-UX результат при минимальном техническом риске.
